{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["dist", "node_modules","src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node -r tsconfig-paths/register src/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
